import{useEffect,useState}from'react';import{BrowserRouter,Navigate,Route,Routes}from'react-router-dom';import{api}from'./lib/api';import type{Bootstrap}from'./types';import{Layout}from'./components/Layout';import{RegisterPage}from'./pages/RegisterPage';import{DataPage}from'./pages/DataPages';import{SettingsPage}from'./pages/SettingsPage';import{SetupPage}from'./pages/SetupPage';
export default function App(){const[data,setData]=useState<Bootstrap>();const load=()=>api.bootstrap().then(setData);useEffect(()=>{load()},[]);if(!data)return <div className="loading">جارٍ فتح قاعدة البيانات…</div>;if(!data.initialized)return <SetupPage done={load}/>;return <BrowserRouter><Routes><Route element={<Layout/>}>{['specialties','period','students','status','finance','ledger'].map(k=><Route path={`/${k}`} element={<DataPage kind={k} data={data}/>}/>)}<Route path="/register" element={<RegisterPage data={data}/>}/><Route path="/settings" element={<SettingsPage/>}/><Route path="*" element={<Navigate to="/register"/>}/></Route></Routes></BrowserRouter>}
